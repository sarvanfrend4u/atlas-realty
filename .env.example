# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/atlas_realty

# Backend
BACKEND_PORT=8000
SECRET_KEY=change-this-to-a-random-secret

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_MAP_STYLE_URL=https://tiles.openfreemap.org/styles/liberty

# AI / LLM (optional — Groq free tier fallback)
GROQ_API_KEY=

# Tile hosting (if using self-hosted Protomaps)
TILES_PATH=./data/tiles/map.pmtiles
